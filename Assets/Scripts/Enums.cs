public enum GameMode
{
    Normal,
    Endless
}

public enum GameState
{
    Init,
    Loading,
    Menu,
    Playing,
    Paused
}

public enum Nationality
{
    a,
    b,
    c
}

public enum DeclarationItem
{
    gun,
    food
}